[project]
name = "khronos-client"
version = "0.1.0"
description = "Python gRPC client library for Khronos workflow scheduler"
requires-python = ">=3.10"
dependencies = [
    "grpcio>=1.62.0",
    "grpcio-tools>=1.62.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
